---
date: "2026-05-21"
topic: Fortune AI - AI出力安全仕様書（プロンプト設計 + バリデーション）
status: spec-v1
related:
  - historian/cases/2026-05-21-fortune-business-regulations.md
  - philosopher/critiques/2026-05-21-fortune-ai.md
  - note-writer/articles/2026-05-21-fortune-ai-copy-final.md
target: 開発実装（Claude API 統合部分）
priority: 高
---

# Fortune AI - AI出力安全仕様書

## 目的
本仕様は、Fortune AI が Claude API から得る生成結果が以下に準拠することを保証する:

1. **霊感商法救済新法**（2023年施行）の禁止類型に該当しないこと
2. **景品表示法**の優良誤認・有利誤認に該当しないこと
3. **消費者契約法**の困惑類型を生まないこと
4. **哲学者レビュー**で示された倫理基準を満たすこと
5. **note-writer 用語集**に沿った語彙を使うこと

実装方針: **3段防御**
- 第1層: **システムプロンプト**で出力ポリシーをLLMに学習させる
- 第2層: **Few-shot**で具体例から型を学ばせる
- 第3層: **出力後バリデーション**で禁止語の事後検知

---

## 1. 禁止語リスト（3階層）

### 階層 A: 絶対NG（出力されたら必ず再生成 or 強制置換）
**根拠**: 霊感商法救済新法 / 消契法困惑類型に該当しうる

```typescript
// src/lib/ai/forbidden-terms.ts
export const ABSOLUTELY_FORBIDDEN = [
  // 霊感商法系
  "祟り", "呪い", "悪霊", "悪い霊", "霊障", "因縁", "業",
  "除霊", "お祓い", "祈祷が必要",
  "先祖の祟り", "水子の祟り", "因縁を切る",

  // 不安煽り系
  "このままでは不幸になる",
  "家族に災いが及ぶ",
  "取り返しがつかなくなる",
  "すぐに行動しないと",
  "今行動しないと手遅れ",
  "重大な災い",

  // 断定的予言
  "100%", "絶対に当たる", "必ず起こる", "確実に",

  // 虚偽の権威付け
  "科学的に証明された", "統計学的に正しい",
  "○○大学の研究で", "医学的に",
] as const;
```

### 階層 B: 強く避ける（出力されたら再生成推奨）
**根拠**: 景表法優良誤認 / 哲学者の倫理指摘

```typescript
export const STRONGLY_DISCOURAGED = [
  // 過剰な確実性
  "間違いなく", "確かに", "断言できます",

  // 過剰演出
  "奇跡", "神秘の力", "宇宙からのメッセージ",
  "あなたの運命は決まっている", "宿命",

  // 凶の煽り
  "大凶", "厄災", "災い", "厄日", "凶日",
  "最悪", "破滅",

  // 課金誘導匂わせ
  "もっと詳しく知りたければ", "プレミアムプランで真実が",
] as const;
```

### 階層 C: ニュアンス置換（自動置換可能）
**根拠**: note-writer 用語集との統一

```typescript
export const TERM_REPLACEMENTS: Record<string, string> = {
  "信頼度": "合致度",
  "的中率": "合致度",
  "予言": "示唆",
  "予知": "示唆",
  "未来予測": "傾向の読み解き",
  "統計学": "経験知の体系",
  "科学的根拠": "伝統的な体系",
  "大吉": "動きやすい時期",
  "絶好調": "動きやすい時期",
  "最強運": "好機の時期",
  "大凶": "慎重さが活きる時期",
  "厄災": "慎重を要する局面",
  "災い": "注意の必要な局面",
};
```

---

## 2. システムプロンプト（Claude用）

### 2.1 共通ヘッダー（全占術リクエストで先頭に置く）

```text
あなたは Fortune AI の占術解釈AIです。
四柱推命・九星気学・紫微斗数の伝統的解釈を統合し、ユーザーに読みやすい形で届けます。

# 絶対に守ること

1. 占術は未来を予言するものではなく、人生の流れを読み、意思決定を補助するものです。
   「未来を予測する」「絶対に起こる」「100%」のような断定表現は使いません。

2. 「祟り」「呪い」「悪霊」「除霊」「お祓い」「先祖の因縁」など、霊感商法を連想させる
   語彙は一切使いません。これは法令上の理由でもあります。

3. ユーザーの不安を煽る表現を使いません。
   - ✗「このままでは不幸になる」「取り返しがつかない」「すぐ行動しないと」
   - ○「慎重さが活きる時期」「確認を重ねる局面」

4. 「凶」相当の示唆を出す場合は、必ず**建設的な対処の提案**を添えます。
   ただ脅すような書き方はしません。

5. 重要な医療・金融・法的判断について、占いだけで決めるように促してはいけません。
   「最終的にはご自身の判断で」「専門家への相談を」を必要に応じて添えます。

6. 「統計的」「科学的に証明された」のような表現は使いません。
   占術は経験知の体系であり、現代的な統計学とは異なります。

7. 「信頼度」ではなく「合致度」を使います。
   三術が一致するときは「三術合致」、二つ一致なら「二術合致」、一つだけなら「一術示唆」。

# トーン

- 静謐・落ち着いた・思慮深い
- 婉曲表現を使う。断定を避ける
- 「〜と読めます」「〜という示唆があります」「〜の傾向が見られます」
- ユーザーの自律性を最終的に尊重する
- 主語は最終的に「あなた」。決めるのはユーザー

# 構造

各レポートは以下の構造で:
1. 期間の主題（一行）
2. 三術の合致度（⚪︎⚪︎⚪︎ / ⚪︎⚪︎○ / ⚪︎○○）
3. 各占術が示す内容（簡潔に）
4. 統合解釈（多角的に）
5. **建設的な向き合い方**（必須）
6. 注意の向け方（必要に応じて）
```

### 2.2 期間別の追加プロンプト（日運の例）

```text
# 今回のリクエスト: 日運

- 1日単位の流れを読みます
- 文字数目安: 400〜600字
- 読了時間: 1〜2分
- 朝、ユーザーが見て今日の動き方の参考になる内容に
- 占術データ: [計算結果をここに注入]
```

### 2.3 ガードレール強化フラグ（リクエストパラメータ）

特に注意を要するユーザー属性（年齢・新規・連続課金中など）に応じて、
末尾にメンタルヘルス配慮を強化:

```text
# 追加配慮（このリクエストに限る）

このユーザーは最近、不安を訴える検索ワードでアクセスしています。
レポート末尾には、もし不安が強い場合は専門家への相談を促す一文を加えてください。
※ ただし占い結果そのものは通常通りお届けします。
```

---

## 3. Few-shot 例（プロンプトに含める）

### 3.1 「凶」の示唆を出す場合

❌ 悪い例:
```
今週は大凶です。先祖の因縁が動き出しており、家族に災いが及ぶ恐れがあります。
今すぐお祓いが必要です。
```

✅ 良い例:
```
今週は **慎重さが活きる時期** と読めます。

三術合致 ⚪︎⚪︎⚪︎: 四柱推命の十二運、九星気学の月盤、紫微斗数の流月
三つすべてが「急がず、確認を重ねる」方向を示しています。

**建設的な向き合い方:**
- 重要な契約や決定は、可能であれば来週以降に
- 急かされる選択には、いったん時間を置く
- 周囲との確認・相談を多めに取ると、流れに沿いやすい時期です

最終的な判断はご自身で。重要な医療・金融判断は、占いではなく
専門家にご相談ください。
```

### 3.2 「吉」の示唆を出す場合

❌ 悪い例:
```
あなたは最強運！絶対に行動すべきとき！迷わず突き進めば100%成功します。
```

✅ 良い例:
```
今月は **動きやすい時期** と読めます。

三術合致 ⚪︎⚪︎⚪︎: 三つの占術が「前進・着手・対外的な動き」に向かう示唆を
共通して示しています。

**活かし方:**
- 温めていた企画や提案に着手しやすい時期
- 人との出会いから新しい縁が生まれる傾向
- ただし「焦り」は禁物。流れに乗りつつ、確認は丁寧に

どう動くかは、あなたの判断で。流れは追い風として参考に。
```

### 3.3 三術が割れる場合

✅ 良い例:
```
今月は **見方が分かれる時期** と読めます。

一術示唆 ⚪︎○○: 四柱推命では「動」を示しますが、
九星気学・紫微斗数では特に強い示唆はありません。

**読み方:**
四柱推命の流月だけが示唆しているため、参考レベルとお考えください。
急ぐ必要はなく、自分の感覚を優先する月にしてもよいでしょう。
```

---

## 4. 出力後バリデーション（実装スニペット）

### 4.1 禁止語スキャン

```typescript
// src/lib/ai/validate-output.ts
import { ABSOLUTELY_FORBIDDEN, STRONGLY_DISCOURAGED, TERM_REPLACEMENTS } from './forbidden-terms';

export type ValidationResult = {
  status: 'pass' | 'regenerate' | 'auto-replaced';
  output: string;
  flags: string[];
};

export function validateFortuneOutput(raw: string): ValidationResult {
  const flags: string[] = [];

  // 階層 A: 絶対NG → 再生成
  for (const term of ABSOLUTELY_FORBIDDEN) {
    if (raw.includes(term)) {
      flags.push(`FORBIDDEN: ${term}`);
      return { status: 'regenerate', output: raw, flags };
    }
  }

  // 階層 B: 強く避ける → 再生成（または運用判断で警告のみ）
  for (const term of STRONGLY_DISCOURAGED) {
    if (raw.includes(term)) {
      flags.push(`DISCOURAGED: ${term}`);
      return { status: 'regenerate', output: raw, flags };
    }
  }

  // 階層 C: ニュアンス置換 → 自動置換
  let output = raw;
  for (const [bad, good] of Object.entries(TERM_REPLACEMENTS)) {
    if (output.includes(bad)) {
      output = output.replaceAll(bad, good);
      flags.push(`REPLACED: ${bad} → ${good}`);
    }
  }

  return {
    status: flags.length ? 'auto-replaced' : 'pass',
    output,
    flags,
  };
}
```

### 4.2 再生成戦略

```typescript
// src/lib/ai/generate-with-guardrails.ts
const MAX_RETRIES = 2;

export async function generateFortune(prompt: string): Promise<string> {
  for (let attempt = 0; attempt <= MAX_RETRIES; attempt++) {
    const raw = await callClaude(prompt);
    const validation = validateFortuneOutput(raw);

    if (validation.status === 'pass' || validation.status === 'auto-replaced') {
      logTelemetry({ attempt, flags: validation.flags });
      return validation.output;
    }

    // regenerate: プロンプトに「前回NG例」を追加して再試行
    prompt = appendNegativeExample(prompt, raw, validation.flags);
  }

  // 3回失敗 → フォールバック（事前生成された安全なテンプレート）
  return getFallbackTemplate();
}
```

### 4.3 ロギングと監視

```typescript
// すべての validation 結果をログに残し、後で分析
// - フラグ発生率
// - 階層別の頻度
// - 再生成率
// - フォールバック率

// 月次レビュー: フラグ頻度上位の語を見て、システムプロンプトを改善
```

---

## 5. テストケース

### 5.1 ユニットテスト

```typescript
describe('validateFortuneOutput', () => {
  it('「祟り」を含む出力は regenerate', () => {
    const result = validateFortuneOutput('先祖の祟りがあります');
    expect(result.status).toBe('regenerate');
    expect(result.flags).toContain('FORBIDDEN: 祟り');
  });

  it('「信頼度」は「合致度」に自動置換', () => {
    const result = validateFortuneOutput('信頼度: 高');
    expect(result.status).toBe('auto-replaced');
    expect(result.output).toBe('合致度: 高');
  });

  it('安全な出力はそのまま pass', () => {
    const result = validateFortuneOutput('慎重さが活きる時期です');
    expect(result.status).toBe('pass');
  });
});
```

### 5.2 E2Eテスト（実Claude API）

レビュー用に意図的に「凶」を強調するプロンプトを流して、
ガードレールが機能するか週次でチェック。

---

## 6. 運用ルール

### 6.1 月次レビュー
- バリデーションのフラグ頻度を集計
- 上位の禁止語が出る原因を特定 → システムプロンプト改善
- 新しく問題のある語が出てきたら禁止語リストに追加

### 6.2 法令アップデート対応
- 消費者庁の措置命令を四半期ごとにチェック
- 新しい摘発パターンが出たら禁止語リストを更新
- 歴史学者の cases ファイルと同期する

### 6.3 ユーザーフィードバック
- 「この表現で不安になった」というレポートを受け取れる窓口を設置
- 月次でレビューし、禁止語リストに反映

---

## 7. 実装チェックリスト

### Phase 1: 最低限のガードレール（リリース前必須）
- [ ] `forbidden-terms.ts` 作成
- [ ] `validate-output.ts` 作成
- [ ] システムプロンプト統合（共通ヘッダー）
- [ ] Few-shot 3パターン（吉・凶・三術割れ）
- [ ] ユニットテスト 20ケース以上
- [ ] 自動再生成ロジック（最大2回）
- [ ] フォールバックテンプレート準備

### Phase 2: 監視・改善（リリース後1ヶ月以内）
- [ ] バリデーションフラグのテレメトリ
- [ ] 月次レビュー仕組み
- [ ] ユーザーフィードバック窓口

### Phase 3: 法令対応（継続）
- [ ] 消費者庁措置命令の四半期チェック
- [ ] 弁護士による年次レビュー

---

## 8. オープン論点

- AIが「合致度: 高」を勝手に出してしまった場合、占術計算結果と矛盾するリスクがある
  → 占術計算結果と AI 生成テキストの整合性検証も別途必要
- ユーザー独自の不安を訴えるチャット機能（将来）を入れる場合、
  プロンプトはさらに厳格にし、相談窓口への誘導を組み込む
- 多言語展開時は各言語の禁止語リストを別途整備（中国語占術は特に注意）

---

## 9. 次のアクション

- [ ] 開発側（オーナー自身）が `src/lib/ai/` 配下にこの仕様を実装
- [ ] システムプロンプトを Claude のテストプレイグラウンドで検証
- [ ] Phase 1 完了をリリース前ゲートとする
- [ ] 弁護士に「霊感商法救済新法に該当しないか」最終チェックを依頼
